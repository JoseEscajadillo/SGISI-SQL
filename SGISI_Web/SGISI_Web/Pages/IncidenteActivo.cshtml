@page
@model IncidenteActivoModel
@{
    ViewData["Title"] = "Incidente-Activo";
}

<h2>Incidente &ndash; Activo</h2>
<p class="text-muted">Tabla de asociación N:M entre incidentes y activos comprometidos.</p>

@if (Model.Error != null)
{
    <div class="alert alert-danger">@Model.Error</div>
}

<partial name="_TablaDatos" model="Model.Tabla" />
