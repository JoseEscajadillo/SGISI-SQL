@page
@model AnalistasModel
@{
    ViewData["Title"] = "Analistas";
}

<h2>Analistas</h2>
<p class="text-muted">
    Enmascaramiento Dinámico de Datos (DDM): el correo, teléfono y nivel de acceso
    se muestran reales solo si tu usuario tiene el permiso <code>UNMASK</code>.
</p>

@if (Model.Error != null)
{
    <div class="alert alert-danger">@Model.Error</div>
}

<partial name="_TablaDatos" model="Model.Tabla" />
